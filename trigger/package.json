{
  "name": "pkgstory-crawl-trigger",
  "version": "0.1.0",
  "description": "Cloudflare Worker cron that reliably dispatches the pkgstory crawl workflow.",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "deploy": "WRANGLER_WRITE_LOGS=false wrangler deploy",
    "deploy:dry": "npm run deploy -- --dry-run",
    "dev": "WRANGLER_WRITE_LOGS=false wrangler dev --test-scheduled",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260901.1",
    "typescript": "^7.0.2",
    "wrangler": "4.128.0"
  },
  "allowScripts": {
    "esbuild": false,
    "fsevents": false,
    "workerd": false
  }
}
