name: Link Check

on:
  schedule:
    - cron: "0 14 * * 1" # every Monday at 14:00 UTC
  workflow_dispatch:

permissions: {}

concurrency:
  group: "link-check"
  cancel-in-progress: true

jobs:
  lychee:
    name: Check links
    permissions:
      contents: read
    uses: starhaven-io/.github/.github/workflows/reusable-link-check.yml@9783ddfb15b9c91b3f53a2a975067ed4937029ad # v2026.09.25.1
    with:
      args: "--config lychee.toml README.md SECURITY.md docs/OPERATIONS.md trigger/README.md"
      build-site: true
      site-directory: "site"
      authenticated-github: false
