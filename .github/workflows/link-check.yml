name: Link Check

on:
  schedule:
    - cron: "0 14 * * 1" # every Monday at 14:00 UTC
  workflow_dispatch:

permissions: {}

concurrency:
  group: link-check
  cancel-in-progress: true

jobs:
  lychee:
    name: Check links
    permissions:
      contents: read
    uses: starhaven-io/.github/.github/workflows/reusable-link-check.yml@20371f85e8e55e28feb2bec0c957514153e0f1d8 # v2026.07.05.9
    with:
      args: "--config lychee.toml README.md trigger/README.md"
      build-site: true
      site-directory: "site"
      authenticated-github: false
