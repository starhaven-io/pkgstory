---
import { sourceLabel } from "../lib/format.ts";

interface Props {
  source: string;
}
const { source } = Astro.props;
const variant = source === "homebrew-cask" ? "chip-cask" : "chip-formula";
---

<span class:list={["chip", variant]}>{sourceLabel(source)}</span>
