{
  "name": "pkgstory-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check:headers": "node scripts/check-security-headers.mjs",
    "prebuild": "node scripts/check-security-headers.mjs",
    "build": "WRANGLER_WRITE_LOGS=false astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "WRANGLER_WRITE_LOGS=false wrangler deploy --config dist/server/wrangler.json",
    "deploy:dry": "npm run deploy -- --dry-run",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.2",
    "@astrojs/rss": "^4.0.19",
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "prettier": "^3.9.7",
    "prettier-plugin-astro": "^1.0.0",
    "wrangler": "4.133.0"
  },
  "allowScripts": {
    "esbuild": false,
    "fsevents": false,
    "workerd": false
  }
}
